import { Canvas, Meta } from "@storybook/blocks";
import Example from "./example.tsx";
import * as ComponentPACP from "./ComponentPACP.stories.tsx";

<Meta of={ComponentPACP} title="ComponentPACP" />

# ComponentPACP

Componente de botón con diferentes especializaciones.

#### Example

<Canvas of={ComponentPACP.Primary} />

## Usage

```ts
import { ComponentPACP } from 'milibreria';

function Example() {
  return (
    <ComponentPACP
      text="Enviar"
      txtcolor='white'
      bgcolor='black'
      height='100px' // Puedes ajustar la altura
      width='200px'  // Puedes ajustar el ancho
      disabled={false}
    />
  );
}

export default Example;
``` 

#### Example

<Canvas of={ComponentPACP.Secondary} />

## Usage

```ts
import {ComponentPACP} from 'milibreria';

function Example() {
  return (
    <ComponentPACP
      text="Limpiar"
      txtcolor='blue'
      bgcolor='pink'
      height='100px' // Puedes ajustar la altura
      width='200px'  // Puedes ajustar el ancho
      disabled={false}
    />
  );
}
export default Example;